using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class child_manager : MonoBehaviour
{
    monster_parents parents;
    // Start is called before the first frame update
    void Start()
    {
        while(true)
        {
            
        }
    }

    // Update is called once per frame
    void Update()
    {

    }
}
